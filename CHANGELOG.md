# v5.1 핵심 검증 로직 수정

기존 v5.0 전체 프로젝트를 다시 배포하지 않고 아래 파일만 교체할 수 있도록 구성했습니다.

## 교체 파일
- `app.py`
- `modules/rule_validator.py`
- `modules/claude_prompt_builder.py`
- `modules/group_resolver.py` (신규)

## 핵심 변경
1. 비교표의 한 제품열을 개별 품목 하나가 아니라 동일 제품군(여러 함량)으로 연결.
2. 비교표의 `[신청 의약품]` 표기를 우선 이용해 신청의약품 그룹을 식별.
3. 자동 매칭이 불확실하면 `확인 권장`으로 표시하고 사용자에게 제품군 연결을 최종 확인/수정하게 함.
4. Python 1차 검증은 여전히 기계적으로 확실한 경우만 자동 판정하고, 서술형 의미 비교는 Claude로 넘김.
5. 숫자·단위 검사는 한국어 단위와 임상 시간/횟수/연령 단위를 지원하고, 동일 제품군의 여러 함량을 고려.
6. Claude 프롬프트를 제품군 단위로 재구성하여 한 비교표 열에 여러 함량이 포함된 실제 심의표 구조를 그대로 검증하도록 개선.
7. 기존 Claude API 호출 금지 원칙과 Secrets 기반 API 키 사용 구조는 변경하지 않음.

## 테스트
기존 selftest: 55 PASS / 0 FAIL

## 적용
기존 GitHub 프로젝트에서 위 파일만 같은 경로로 덮어쓰세요. `group_resolver.py`는 신규 파일이므로 추가해야 합니다.
